import { SvgRefDirective } from './svg-ref.directive';

describe('SvgRefDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgRefDirective();
    expect(directive).toBeTruthy();
  });
});
